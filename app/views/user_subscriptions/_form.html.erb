<%= form_for([current_user, @user_subscription]) do |f| %>
  <% if @user_subscription.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@user_subscription.errors.count, "error") %> prohibited this user_subscription from being saved:
      </h2>
      <ul>
      <% @user_subscription.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :phone %><br />
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label :sms_id %><br />
    <%= f.text_field :sms_id %>
  </div>
  <div class="field">
    <%= f.label :is_active %><br />
    <%= f.check_box :is_active %>
  </div>
  <div class="field">
    <%= f.label :remind_hour %><br />
    <%= f.number_field :remind_hour %>
  </div>
  <div class="field">
    <%= f.label :remind_minute %><br />
    <%= f.number_field :remind_minute %>
  </div>
 <div class="actions">
    <%= f.submit 'Update', :class => 'btn btn-primary'%>
  </div>
<% end %>
